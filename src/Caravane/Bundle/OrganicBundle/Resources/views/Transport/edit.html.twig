{% extends '::base.html.twig' %}

{% block body -%}
    <div class="row">
        <div class="col-md-12">
        {% block form_header %}
            <form action="{{ path('transport_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
         {% endblock form_header %}


         <div class="row">
            <div class="col-md-12">
                <div class="actions">
                    <button type="submit" class="btn btn-inverse">Save</button>
                    <a href="{{ path('transport') }}" class="btn">
                            Back to the list
                    </a>
                </div>
            </div>
        </div>


        {#<input type="hidden" name="_method" value="PUT" />#}
        {{ form_widget(edit_form) }}

    </form>
    {#
            <ul class="record_actions">
        <li>
            <a href="{{ path('transport') }}">
                Back to the list
            </a>
        </li>
        <li>
            <form action="{{ path('transport_delete', { 'id': entity.id }) }}" method="post">
                <input type="hidden" name="_method" value="DELETE" />
                {{ form_widget(delete_form) }}
                <button type="submit">Delete</button>
            </form>
        </li>
    </ul>
    #}
        </div>
    </div>
{% endblock %}

